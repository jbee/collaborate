package se.jbee.track.ui.ctrl;

public enum Action {

	//GETS
	view, 
	
	// user/foo/as/bar => looking at user "foo" as the user "bar" would see it (but with the rights of you whoever you are)
	
	
	// PUTS/POSTS
	enlist, approach, abandon, stress
}
